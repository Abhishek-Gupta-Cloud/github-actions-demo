name: My First Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello GitHub Actions"
      - name: again checkin
        run : echo "this is my first hii and hello"
